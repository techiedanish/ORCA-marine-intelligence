# Only needed if you run `npm run dev` against a separately-running backend
# during iterative frontend development. Not needed for the single-port
# production build (relative "/api/v1" is used automatically).
NEXT_PUBLIC_ORCA_API_BASE=http://localhost:8000/api/v1

# REQUIRED for the map to render. Get a free key from Google Cloud Console and
# paste it below — full step-by-step instructions are in the root README.md
# ("Getting a free Google Maps API key"). This is baked in at BUILD time (this
# app is statically exported), so rebuild after changing it:
#   python run.py --rebuild
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=AIzaSyD4zkCfONNgXVw8J3VPDh1iVAvA4nrbofk
